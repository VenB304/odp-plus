[
    {
        "odpClient": {
            "tag": "Host",
            "contents": { "id": "ODP-Host" }
        },
        "note": "Host connects to JDN directly; Room ID is auto-detected from the JDN WebSocket. P2P peer ID is deterministic: odp-{roomId}."
    },
    {
        "odpClient": {
            "tag": "Follower",
            "contents": { "hostToFollow": "1999" }
        },
        "note": "Follower connects to the Host via PeerJS (WebRTC). The hostToFollow value is the Room ID entered in the popup."
    }
]
